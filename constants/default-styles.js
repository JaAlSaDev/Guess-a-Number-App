import { StyleSheet } from "react-native";



// This is a global style for the whole app
// You can either use seperate components that have default styles,
// or use a constant file for styles
export default StyleSheet.create({
    bodyText: {
        fontFamily: 'open-sans'
    },
    title: {
        fontFamily: 'open-sans-bold',
        fontSize: 18
    }
})